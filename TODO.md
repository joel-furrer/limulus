# ToDo
- leave only main logic in limulus.go
- Refactor limulus.go into packages/folders 'lexer' ( tokenizer ) and package 'parser' ( ast )
- add package ir ( includes logic for generating intermediate representation from AST )
