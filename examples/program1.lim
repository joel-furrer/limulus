let x = 5 + 3
let y = 10 - 2 * 4
let z = ( 1 + 2 ) * ( 3 + 4 )
let a = 5u + 10u
let b = 3.5 + 4.1
let c = x + y - z * a / b
let result = ( x + y ) * ( z - a ) + ( b / 2.0 )

cout x
cout y
cout z
cout a
cout b
cout c
cout result

let complexExpr = ( ( x + 2 ) * ( y - 3 ) + 7u ) / ( z + 1.5 )
cout complexExpr

let mixedTypes = x + 3.2 - 5u * ( y + 2 )
cout mixedTypes

let nested = ( ( ( 1 + 2 ) * 3 ) - ( 4 / 2 ) ) + ( 5u * ( x - y ) )
cout nested
